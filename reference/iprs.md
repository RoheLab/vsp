# Calculate IPR for all singular vectors in a list.

Calculate IPR for all singular vectors in a list.

## Usage

``` r
iprs(s)
```

## Arguments

- s:

  A list containing `u` and `v` matrices of singular vectors.

## Value

A tibble with IPR values for each singular vector in `u` and `v`.
